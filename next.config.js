/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['smswedhyzarlietowqun.supabase.co'],
  },
    typescript: {
          ignoreBuildErrors: true,
    },
}

module.exports = nextConfig
